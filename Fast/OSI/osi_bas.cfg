MEMORY {
	ZP:			start = $0000, size = $0100, type = rw;
	PAD:        start = $8000, size = $4000, fill = yes, fillval=$FF, file = %O;
	BASROM:		start = $C000, size = $3E00, fill = yes, fillval=$FF, file = %O;
	IOHANDLER:	start = $FE00, size = $1FA, fill = yes, fillval=$FF, file = %O;
	VECTS:		start = $FFFA, size = $6, fill = yes, fillval=$FF, file = %O;
}

SEGMENTS {
	ZEROPAGE:	load = ZP, type = zp;
	PAD:        load = PAD, type = ro;
	CODE:		load = BASROM,	type = ro;
	IOHANDLER:	load = IOHANDLER,	type = ro;
	VECTS:		load = VECTS,	type = ro;
}

